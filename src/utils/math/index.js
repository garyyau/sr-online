// Core
import clamp from './clamp'
import * as ease from './easing'
import lerp2d from './lerp2d'
import distance2d from './distance2d'

// Extend
import pointOnCircle from './pointOnCircle'
import pointOnPath from './pointOnPath'
import pointRotate2d from './pointRotate2d'

// Constants
const TWO_PI = Math.PI * 2
const HALF_PI = Math.PI * 0.5

export {
  // Constants
  TWO_PI,
  HALF_PI,
  // Core
  clamp,
  ease,
  lerp2d,
  distance2d,
  // Extend
  pointOnCircle,
  pointOnPath,
  pointRotate2d,
}
